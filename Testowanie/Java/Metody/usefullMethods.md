# usefullMethods

# Użyteczne metody

`Random random = new Random();` - metoda generuje losową liczbę. W metodzie możemy ustawić zakres liczb.

`Comparator.naturalOrder()` -

`List.sort(Comparator.comparing(Student::getName));`